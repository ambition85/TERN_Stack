pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
				bat 'echo Build stage started
				bat 'echo'
				bat 'npm --version'
				bat 'echo $WORKSPACE'
            }
        }
    }
}
